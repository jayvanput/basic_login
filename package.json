{
  "name": "basic_login",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "mocha",
    "dev": "cd server && nodemon server",
    "start": "node server --prefix server",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm install --prefix server && npm run build --prefix client"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "mocha": "^8.3.0",
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "concurrently": "^6.0.0"
  }
}
